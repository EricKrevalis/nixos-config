# allow 44.1k beside the 48k default, keeps 44.1k sources off the resampler
# resample.quality 14 is the max, only matters on a forced rate switch
context.properties = {
    default.clock.rate          = 48000
    default.clock.allowed-rates = [ 44100 48000 ]
    resample.quality            = 14
}
